</br>
    {% if endL > 0 %}
        {% set combattre = null %} 
                    <p>You escape the Black Bear.
                        <a  class="col-12 mt-5"
                            href="
                            {{ path('page_106', {'heroId':heroId }) }}
                            "
                        >
                            <img class="fleches" src="{{ asset ('assets/img/ArrowR.png') }}" />
                        </a> 
                        {# turn to 106. #}
                    </p>
                    </br>
    {% endif %}