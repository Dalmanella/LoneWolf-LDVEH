</br>
       {% if endE <= 0 and endL > 0 %}
            {% set combattre = null %} 
                <p>You kill all three of them. 
                </br>  
                       <a  class="col-12 mt-5"
                            href="
                            {{ path('page_62', {'heroId':heroId }) }}
                            "
                        >
                            <img class="fleches" src="{{ asset ('assets/img/ArrowR.png') }}" />
                        </a> 
                        {# turn to 62. #}
                    
                </p>
        {% endif %}