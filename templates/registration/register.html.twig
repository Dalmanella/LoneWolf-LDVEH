{% extends 'base.html.twig' %}

{% block title %}LoneWolf Inscription{% endblock %}

{% block header %}
    {{ include('entete/_entetemenu.html.twig') }}    
{% endblock %}

{% block body %}
    <p class="connexion mt-3 mb-1" style="font-family:'Dr Sugiyama', cursive;" >Inscription</p>
    <div class="form">
    {{ form_start(inscriptionForm) }}
        {{ form_row(inscriptionForm.pseudo) }}
        {# {{ form_row(inscriptionForm.email) }} #}
        {{ form_row(inscriptionForm.plainPassword, {label: 'Password'}) }}
       
    
        <button type="submit" class="btn btn-primary p-0 mb-5"><img class="fleches" src="{{ asset ('assets/img/pawprint-brown.png') }}" alt="validation button"/></button>
    {{ form_end(inscriptionForm) }}
    </div>
    <a href="{{ path('accueil') }}" class="text-info mt-5 display-3">Connexion</a>

{% endblock %}
{% block footer %}
    <div class="sticky-bottom">
            <img class="fleches mt-5" src="{{ asset ('assets/img/decoBas.png') }}" />
            </br></br>
    </div>
{% endblock %}

