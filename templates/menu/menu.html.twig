{% extends 'base.html.twig' %}

{% block title %}LoneWolf menu{% endblock %}

{% block header %}
    {{ include('entete/_entetemenu.html.twig') }}    
{% endblock %}

{% block body %}

    <div class="mt-3 px-0 " id="menuList">
        <!-- Option continuer à conditionner à l'éxistence d'un hero sur le compte utilisateur 
            if (id_hero){ affichage option } -->
        {% if heroes %}  
        {# <button type="button" class="btn-menu" onclick="document.location=`{{ path('continue') }}`"> <img class=""style="max-width:60px;" src="{{ asset ('assets/img/ico-openbook.png') }}" /><p>Continuer</p></button> #}
        <a class="d-flex text-decoration-none justify-content-center icone" onclick="document.location=`{{ path('continue') }}`">
            <div class="">
                <img class="img-fluid" src="{{ asset ('assets/img/ico-openbook.png') }}"/>
                <p class="">Continuer</p>
            </div>
            
        </a>

        {% endif %} 
        <!-- Options toujours présentes -->
         {% if heroesNum < 3 %}   
        {# <button type="button" class="btn-menu" onclick="document.location=`{{ path('nouvelle') }}`"> <img class=""style="max-width:60px;" src="{{ asset ('assets/img/ico-books.png') }}" /><p>Nouvelle Aventure</p></button> #}
        <a class="d-flex text-decoration-none justify-content-center icone" onclick="document.location=`{{ path('nouvelle') }}`">
            <div class="">
                <img class="img-fluid" src="{{ asset ('assets/img/ico-books.png') }}" />
                <p class="">Nouvelle Aventure</p>
            </div>
            
        </a>
        {% endif %}

        {# <button type="button" class="btn-menu" onclick="document.location=`{{ path('A_propos') }}`"> <img class=""style="max-width:60px;" src="{{ asset ('assets/img/ico-quill.png') }}" /><p>A Propos</p></button> #}
        <a class="d-flex text-decoration-none justify-content-center icone" onclick="document.location=`{{ path('A_propos') }}`">
            <div class="">
                <img class="img-fluid " src="{{ asset ('assets/img/ico-quill.png') }}" />
                <p class="">A Propos</p>
            </div>
            
        </a>

        <!-- Pour gerer la musique -->
            <a class="d-flex text-decoration-none justify-content-center icone" href="{{ path('music')}}" target="_blank">
            <div class="">
                <img class="img-fluid" src="{{ asset ('assets/img/music.png') }}" />
                <p class="">Musique</p>
            </div>
            
        </a>
    
    
   
        {% if admin is same as true %}    
        <a class="d-flex text-decoration-none justify-content-center icone" onclick="document.location=`{{ path('gestion') }}`">
            <div class="">
                <img class="img-fluid" src="{{ asset ('assets/img/ico-gear.png') }}" />
                <p class="">Gestion</p>
            </div>
            
        </a>
        {% endif %}
        
       
    </div>
{% endblock %}
{% block footer %}
        <div class="sticky-bottom">
            <img class="fleches mt-5" src="{{ asset ('assets/img/decoBas.png') }}" />
            </br></br>
{% endblock %}
